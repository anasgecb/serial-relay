We have build a relay controlled solenoid valve which is connected using a arduino board.
internal switch is used with help of serial monitor which gets switched on when "A" is pressed and gets turns Off when "A" is once again switched
